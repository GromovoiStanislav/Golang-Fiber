## Fiber with SQLite (Gorm) REST API simple example

### Setup:

```
go mod init fiber-example

go get -u github.com/gofiber/fiber/v2
go get -u gorm.io/gorm
go get -u gorm.io/driver/sqlite
```

### Start the server:

```
go run .
```
